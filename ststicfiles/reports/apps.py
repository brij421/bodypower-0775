from django.apps import AppConfig

class ReportsConfig(AppConfig):
    name = 'reports'
    default_auto_field = 'django.db.models.BigAutoField'  # Set default primary key field type to BigAutoField
